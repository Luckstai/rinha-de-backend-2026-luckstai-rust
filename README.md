# Submission Branch

Esta branch contem apenas os arquivos necessarios para executar a submissao da Rinha de Backend 2026.

- `docker-compose.yml` na raiz
- binario precompilado da API
- configuracao do HAProxy
- recursos de runtime estritamente necessarios

O codigo-fonte fica apenas na branch `main`.
